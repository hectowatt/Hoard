{
  "name": "Hoard Dev Container",
  "dockerComposeFile": "../docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/app",
  "postCreateCommand": "cd /app/src/backend && npm install && npm install && cd /app/src/frontend && npm install && git config --global user.name hectowatt && git config --global user.email w477.dev@gmail.com",
  "remoteEnv": {
    "DATABASE_URL": "postgres://hoard_user:hoard_password@db:5432/hoard_db"
  }
}